// Code generated by cssgen. DO NOT EDIT.
//
// Component: Graph
// Generated: 2026-02-06 08:48:13

package cssgen

// Graph component styles
//
// For the full CSS class registry, see styles.gen.go

// **Visual:**
// - background: `var(--ui-color-surface-container)` 🎨
// - border: `var(--ui-border-sm) solid var(--ui-color-outline)` 🎨
// - border-radius: `var(--ui-radius-lg)` 🎨
// **Layout:**
// - align-items: `center`
// - display: `flex`
// - flex: `1`
// - justify-content: `center`
// - min-height: `400px`
const GraphContainer = "graph-container"

// **Visual:**
// - border-top: `var(--ui-border-sm) solid var(--ui-color-outline-variant)` 🎨
// **Layout:**
// - margin-top: `var(--ui-space-lg)` 🎨
// - padding-top: `var(--ui-space-sm)` 🎨
const GraphEdgesInfo = "graph-edges-info"

// **Visual:**
// - background: `var(--ui-color-surface-container-high)` 🎨
// - border-radius: `var(--ui-radius-sm)` 🎨
// - color: `var(--ui-color-surface-on)` 🎨
// **Layout:**
// - display: `flex`
// - flex-wrap: `wrap`
// - gap: `var(--ui-space-sm)` 🎨
// - list-style: `none`
// - margin: `var(--ui-space-sm) 0 0 0` 🎨
// - [+1 more layout properties]
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-sm)` 🎨
const GraphEdgesList = "graph-edges-list"

// **Visual:**
// - color: `var(--ui-color-surface-variant-on)` 🎨
// **Layout:**
// - padding: `48px`
// **Typography:**
// - text-align: `center`
const GraphEmpty = "graph-empty"

// **Layout:**
// - margin: `0 0 var(--ui-space-xs) 0` 🎨
// - margin-bottom: `var(--ui-space-sm)` 🎨
// **Typography:**
// - font-size: `var(--ui-type-size-2xl)` 🎨
// - font-weight: `var(--ui-weight-semibold)` 🎨
const GraphHeader = "graph-header"

// **Layout:**
// - display: `flex`
// - gap: `var(--ui-space-sm)` 🎨
const GraphLegend = "graph-legend"

// **Base:** .graph-legend
// **Context:** Use with .graph-legend for proper styling
// **Overrides:** 3 properties (border-radius, height, width)
//
// **Visual:**
// - border-radius: `3px`
// **Layout:**
// - height: `var(--ui-space-md)` 🎨
// - width: `var(--ui-space-md)` 🎨
const GraphLegendColor = "graph-legend__color"

// **Base:** .graph-legend__color
// **Context:** Use with .graph-legend__color for proper styling
// **Overrides:** 1 properties (background)
//
// **Visual:**
// - background: `var(--ui-color-secondary)` 🎨
const GraphLegendColorIncremental = "graph-legend__color--incremental"

// **Base:** .graph-legend__color
// **Context:** Use with .graph-legend__color for proper styling
// **Overrides:** 1 properties (background)
//
// **Visual:**
// - background: `var(--ui-color-tertiary)` 🎨
const GraphLegendColorTable = "graph-legend__color--table"

// **Base:** .graph-legend__color
// **Context:** Use with .graph-legend__color for proper styling
// **Overrides:** 1 properties (background)
//
// **Visual:**
// - background: `var(--ui-color-primary)` 🎨
const GraphLegendColorView = "graph-legend__color--view"

// **Base:** .graph-legend
// **Context:** Use with .graph-legend for proper styling
// **Overrides:** 3 properties (align-items, font-size, gap)
//
// **Layout:**
// - align-items: `center`
// - display: `flex`
// - gap: `6px`
// **Typography:**
// - font-size: `var(--ui-type-size-sm)` 🎨
const GraphLegendItem = "graph-legend__item"

// **Visual:**
// - background: `var(--ui-color-surface-container)` 🎨
// - border: `2px solid var(--ui-color-outline-variant)` 🎨
// - border-radius: `var(--ui-radius-lg)` 🎨
// **Layout:**
// - cursor: `pointer`
// - display: `flex`
// - flex-direction: `column`
// - gap: `var(--ui-space-xs)` 🎨
// - padding: `var(--ui-space-md)` 🎨
// **Effects:**
// - transition: `all var(--ui-duration-fast) var(--ui-ease-default)` 🎨
//
// **Interactions:**
// - `:hover`: Changes background to `var(--ui-color-surface-container-high)`, border-color to `var(--ui-color-primary)`, transform to `translateY(-2px)`
const GraphNode = "graph-node"

// **Base:** .graph-node
// **Context:** Use with .graph-node for proper styling
// **Overrides:** 2 properties (border-left-color, border-left-width)
//
// **Visual:**
// - border-left-color: `var(--ui-color-secondary)` 🎨
// - border-left-width: `var(--ui-space-xs)` 🎨
const GraphNodeIncremental = "graph-node--incremental"

// **Base:** .graph-node
// **Context:** Use with .graph-node for proper styling
// **Overrides:** 2 properties (font-size, font-weight)
//
// **Typography:**
// - font-size: `var(--ui-type-size-base)` 🎨
// - font-weight: `var(--ui-weight-medium)` 🎨
const GraphNodeLabel = "graph-node__label"

// **Base:** .graph-node
// **Context:** Use with .graph-node for proper styling
// **Overrides:** 2 properties (border-left-color, border-left-width)
//
// **Visual:**
// - border-left-color: `var(--ui-color-surface-variant-on)` 🎨
// - border-left-width: `var(--ui-space-xs)` 🎨
const GraphNodeSource = "graph-node--source"

// **Base:** .graph-node
// **Context:** Use with .graph-node for proper styling
// **Overrides:** 2 properties (border-left-color, border-left-width)
//
// **Visual:**
// - border-left-color: `var(--ui-color-tertiary)` 🎨
// - border-left-width: `var(--ui-space-xs)` 🎨
const GraphNodeTable = "graph-node--table"

// **Base:** .graph-node
// **Context:** Use with .graph-node for proper styling
// **Overrides:** 3 properties (color, font-size, text-transform)
//
// **Visual:**
// - color: `var(--ui-color-surface-variant-on)` 🎨
// **Typography:**
// - font-size: `var(--ui-type-size-xs)` 🎨
// - text-transform: `uppercase`
const GraphNodeType = "graph-node__type"

// **Base:** .graph-node
// **Context:** Use with .graph-node for proper styling
// **Overrides:** 2 properties (border-left-color, border-left-width)
//
// **Visual:**
// - border-left-color: `var(--ui-color-primary)` 🎨
// - border-left-width: `var(--ui-space-xs)` 🎨
const GraphNodeView = "graph-node--view"

// **Layout:**
// - display: `grid`
// - gap: `var(--ui-space-md)` 🎨
// - grid-template-columns: `repeat(auto-fill, minmax(180px, 1fr))`
const GraphNodes = "graph-nodes"

// **Layout:**
// - display: `flex`
// - flex: `1`
// - flex-direction: `column`
// - overflow: `hidden`
const GraphPage = "graph-page"

// **Layout:**
// - width: `100%`
const GraphSvgContainer = "graph-svg-container"

// **Layout:**
// - display: `flex`
// - flex-direction: `column`
// - height: `100%`
// - width: `100%`
const GraphView = "graph-view"

// **Base:** .graph-view
// **Context:** Use with .graph-view for proper styling
// **Overrides:** 3 properties (flex, overflow, padding)
//
// **Layout:**
// - flex: `1`
// - overflow: `auto`
// - padding: `var(--ui-space-sm)` 🎨
const GraphViewCanvas = "graph-view__canvas"

// **Base:** .graph-view
// **Context:** Use with .graph-view for proper styling
// **Overrides:** 3 properties (background, border-top, padding)
//
// **Visual:**
// - background: `var(--ui-color-surface-container-high)` 🎨
// - border-top: `var(--ui-border-sm) solid var(--ui-color-outline-variant)` 🎨
// **Layout:**
// - padding: `var(--ui-space-md) var(--ui-space-sm)` 🎨
const GraphViewLegend = "graph-view__legend"

// **Base:** .graph-view
// **Context:** Use with .graph-view for proper styling
// **Overrides:** 4 properties (border-top, color, font-size, padding)
//
// **Visual:**
// - border-top: `var(--ui-border-sm) solid var(--ui-color-outline-variant)` 🎨
// - color: `var(--ui-color-surface-variant-on)` 🎨
// **Layout:**
// - padding: `var(--ui-space-sm) var(--ui-space-sm)` 🎨
// **Typography:**
// - font-size: `var(--ui-type-size-sm)` 🎨
const GraphViewStats = "graph-view__stats"

