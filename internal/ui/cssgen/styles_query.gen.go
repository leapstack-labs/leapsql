// Code generated by cssgen. DO NOT EDIT.
//
// Component: Query
// Generated: 2026-02-06 10:18:37

package cssgen

// Query component styles
//
// For the full CSS class registry, see styles.gen.go

// **Layout:**
// - padding: `var(--spacing-sm)`
const CmContent = "cm-content"

// **Visual:**
// - border-radius: `var(--ui-radius-md)` 🎨
// **Layout:**
// - height: `100%`
// - max-height: `300px`
// - min-height: `180px`
// - overflow: `hidden`
const CmEditor = "cm-editor"

// **Visual:**
// - outline: `2px solid var(--ui-color-primary)` 🎨
// **Layout:**
// - outline-offset: `-2px`
const CmFocused = "cm-focused"

// **Visual:**
// - background: `var(--ui-color-outline)` 🎨
// - border-radius: `var(--ui-radius-sm)` 🎨
// **Layout:**
// - height: `var(--ui-space-sm)` 🎨
// - width: `var(--ui-space-sm)` 🎨
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-sm)` 🎨
const CmScroller = "cm-scroller"

// **Visual:**
// - background: `var(--ui-color-surface-container)` 🎨
// - border: `var(--ui-border-sm) solid var(--ui-color-outline)` 🎨
// - border-radius: `var(--ui-radius-md)` 🎨
// - outline: `none`
// **Layout:**
// - height: `100%`
// - max-height: `300px`
// - min-height: `180px`
// - overflow: `hidden`
// - padding: `var(--spacing-sm)`
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-sm)` 🎨
const QueryEditor = "query-editor"

// **Base:** .query-editor
// **Context:** Use with .query-editor for proper styling
// **Overrides:** 5 properties (background, border, color, resize, width)
//
// **Visual:**
// - background: `transparent`
// - border: `none`
// - color: `var(--ui-color-surface-variant-on)` 🎨
// - outline: `none`
// **Layout:**
// - height: `100%`
// - min-height: `180px`
// - padding: `var(--spacing-sm)`
// - resize: `vertical`
// - width: `100%`
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-sm)` 🎨
const QueryEditorTextarea = "query-editor__textarea"

// **Layout:**
// - display: `grid`
// - gap: `var(--spacing-md)`
// - grid-template-columns: `1fr`
// - height: `100%`
// - overflow: `hidden`
const QueryPage = "query-page"

// **Base:** .query-page
// **Context:** Use with .query-page for proper styling
// **Overrides:** 10 properties (align-items, background, border-radius, display, font-family, font-size, gap, margin-left, opacity, padding)
//
// **Visual:**
// - background: `var(--ui-color-surface-container-high)` 🎨
// - border-radius: `var(--ui-radius-sm)` 🎨
// - opacity: `0.7`
// **Layout:**
// - align-items: `center`
// - display: `flex`
// - gap: `var(--spacing-sm)`
// - margin-left: `var(--spacing-xs)`
// - padding: `2px 6px`
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-xs)` 🎨
const QueryPageActions = "query-page__actions"

// **Base:** .query-page
// **Context:** Use with .query-page for proper styling
// **Overrides:** 3 properties (display, flex-direction, gap)
//
// **Layout:**
// - display: `flex`
// - flex-direction: `column`
// - gap: `var(--spacing-xs)`
const QueryPageEditorHeader = "query-page__editor-header"

// **Base:** .query-page
// **Context:** Use with .query-page for proper styling
// **Overrides:** 3 properties (display, flex-direction, gap)
//
// **Layout:**
// - display: `flex`
// - flex-direction: `column`
// - gap: `var(--spacing-sm)`
const QueryPageEditorSection = "query-page__editor-section"

// **Base:** .query-page
// **Context:** Use with .query-page for proper styling
// **Overrides:** 3 properties (display, flex-direction, padding)
//
// **Layout:**
// - display: `flex`
// - flex-direction: `column`
// - gap: `var(--spacing-md)`
// - overflow: `hidden`
// - padding: `var(--spacing-lg)`
const QueryPageMain = "query-page__main"

// **Base:** .query-page
// **Context:** Use with .query-page for proper styling
// **Overrides:** 5 properties (background, border-left, display, flex-direction, overflow-y)
//
// **Visual:**
// - background: `var(--ui-color-surface-container-low)` 🎨
// - border-left: `1px solid var(--ui-color-outline-variant)` 🎨
// **Layout:**
// - display: `none`
// - flex-direction: `column`
// - overflow-y: `auto`
const QueryPageSidebar = "query-page__sidebar"

// **Base:** .query-page
// **Context:** Use with .query-page for proper styling
// **Overrides:** 4 properties (color, font-size, font-weight, margin)
//
// **Visual:**
// - color: `var(--ui-color-surface-on)` 🎨
// **Layout:**
// - margin: `0`
// **Typography:**
// - font-size: `var(--ui-type-size-lg)` 🎨
// - font-weight: `var(--ui-weight-semibold)` 🎨
const QueryPageTitle = "query-page__title"

// **Layout:**
// - flex: `1`
// - min-height: `200px`
// - overflow: `auto`
const QueryResults = "query-results"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 1 properties (font-weight)
//
// **Typography:**
// - font-weight: `var(--ui-weight-medium)` 🎨
const QueryResultsCount = "query-results__count"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 5 properties (align-items, color, display, height, justify-content)
//
// **Visual:**
// - color: `var(--ui-color-surface-variant-on)` 🎨
// **Layout:**
// - align-items: `center`
// - display: `flex`
// - height: `100px`
// - justify-content: `center`
const QueryResultsEmpty = "query-results__empty"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 8 properties (align-items, background, border, border-radius, color, display, gap, padding)
//
// **Visual:**
// - background: `var(--ui-color-error-container)` 🎨
// - border: `var(--ui-border-sm) solid var(--ui-color-error)` 🎨
// - border-radius: `var(--ui-radius-md)` 🎨
// - color: `var(--ui-color-error-container-on)` 🎨
// **Layout:**
// - align-items: `flex-start`
// - display: `flex`
// - gap: `var(--spacing-sm)`
// - padding: `var(--spacing-md)`
const QueryResultsError = "query-results__error"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 2 properties (flex-shrink, font-weight)
//
// **Layout:**
// - flex-shrink: `0`
// **Typography:**
// - font-weight: `bold`
const QueryResultsErrorIcon = "query-results__error-icon"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 4 properties (font-family, font-size, white-space, word-break)
//
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-sm)` 🎨
// - white-space: `pre-wrap`
// - word-break: `break-word`
const QueryResultsErrorMessage = "query-results__error-message"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 6 properties (align-items, color, display, font-size, justify-content, padding)
//
// **Visual:**
// - color: `var(--ui-color-surface-variant-on)` 🎨
// **Layout:**
// - align-items: `center`
// - display: `flex`
// - justify-content: `space-between`
// - padding: `var(--spacing-sm) 0`
// **Typography:**
// - font-size: `var(--ui-type-size-sm)` 🎨
const QueryResultsHeader = "query-results__header"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 16 properties (background, border-bottom, border-collapse, font-family, font-size, font-weight, max-width, overflow, padding, position, text-align, text-overflow, top, white-space, width, z-index)
//
// **Visual:**
// - background: `var(--ui-color-surface-container-high)` 🎨
// - border-bottom: `var(--ui-border-sm) solid var(--ui-color-outline-variant)` 🎨
// - border-collapse: `collapse`
// **Layout:**
// - max-width: `300px`
// - overflow: `hidden`
// - padding: `var(--spacing-xs) var(--spacing-sm)`
// - position: `sticky`
// - top: `0`
// - [+2 more layout properties]
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-sm)` 🎨
// - font-weight: `var(--ui-weight-semibold)` 🎨
// - text-align: `left`
// - text-overflow: `ellipsis`
// - [+1 more typography properties]
const QueryResultsTable = "query-results__table"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 3 properties (border, border-radius, max-height)
//
// **Visual:**
// - border: `var(--ui-border-sm) solid var(--ui-color-outline)` 🎨
// - border-radius: `var(--ui-radius-md)` 🎨
// **Layout:**
// - max-height: `calc(100vh - 450px)`
// - overflow: `auto`
const QueryResultsTableWrapper = "query-results__table-wrapper"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 1 properties (font-family)
//
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
const QueryResultsTime = "query-results__time"

// **Base:** .query-results
// **Context:** Use with .query-results for proper styling
// **Overrides:** 2 properties (color, margin-left)
//
// **Visual:**
// - color: `var(--ui-color-error)` 🎨
// **Layout:**
// - margin-left: `var(--spacing-xs)`
const QueryResultsTruncated = "query-results__truncated"

// **Layout:**
// - padding: `var(--spacing-md)`
const QuerySearchResults = "query-search-results"

// **Base:** .query-search-results
// **Context:** Use with .query-search-results for proper styling
// **Overrides:** 4 properties (color, display, font-size, margin-top)
//
// **Visual:**
// - color: `var(--ui-color-surface-variant-on)` 🎨
// **Layout:**
// - display: `block`
// - margin-top: `var(--spacing-xs)`
// **Typography:**
// - font-size: `var(--ui-type-size-sm)` 🎨
const QuerySearchResultsDesc = "query-search-results__desc"

// **Base:** .query-search-results
// **Context:** Use with .query-search-results for proper styling
// **Overrides:** 3 properties (border-bottom, cursor, padding)
//
// **Visual:**
// - border-bottom: `var(--ui-border-sm) solid var(--ui-color-outline-variant)` 🎨
// **Layout:**
// - cursor: `pointer`
// - padding: `var(--spacing-sm)`
//
// **Interactions:**
// - `:hover`: Changes background to `var(--ui-color-surface-container-high)`
const QuerySearchResultsItem = "query-search-results__item"

// **Base:** .query-search-results
// **Context:** Use with .query-search-results for proper styling
// **Overrides:** 3 properties (list-style, margin, padding)
//
// **Layout:**
// - list-style: `none`
// - margin: `0`
// - padding: `0`
const QuerySearchResultsList = "query-search-results__list"

// **Base:** .query-search-results
// **Context:** Use with .query-search-results for proper styling
// **Overrides:** 2 properties (display, font-weight)
//
// **Layout:**
// - display: `block`
// **Typography:**
// - font-weight: `var(--ui-weight-semibold)` 🎨
const QuerySearchResultsName = "query-search-results__name"

// **Base:** .query-search-results
// **Context:** Use with .query-search-results for proper styling
// **Overrides:** 4 properties (color, display, font-family, font-size)
//
// **Visual:**
// - color: `var(--ui-color-surface-variant-on)` 🎨
// **Layout:**
// - display: `block`
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-xs)` 🎨
const QuerySearchResultsPath = "query-search-results__path"

// **Visual:**
// - color: `var(--ui-color-surface-variant-on)` 🎨
// **Layout:**
// - cursor: `pointer`
// - margin-bottom: `var(--spacing-md)`
// - padding: `var(--spacing-xs) 0`
// - user-select: `none`
// **Typography:**
// - font-size: `var(--ui-type-size-sm)` 🎨
// - font-weight: `var(--ui-weight-semibold)` 🎨
//
// **Interactions:**
// - `:hover`: Changes color to `var(--ui-color-surface-on)`
const QuerySidebarGroup = "query-sidebar__group"

// **Visual:**
// - background: `var(--ui-color-surface-container-high)` 🎨
// - border-radius: `var(--ui-radius-sm)` 🎨
// - color: `var(--ui-color-surface-on)` 🎨
// **Layout:**
// - align-items: `center`
// - display: `flex`
// - flex-shrink: `0`
// - height: `18px`
// - justify-content: `center`
// - [+1 more layout properties]
// **Typography:**
// - font-size: `var(--ui-type-size-xs)` 🎨
// - font-weight: `bold`
const QuerySidebarIcon = "query-sidebar__icon"

// **Visual:**
// - background: `none`
// - border: `none`
// - border-radius: `var(--ui-radius-sm)` 🎨
// - color: `var(--ui-color-surface-on)` 🎨
// **Layout:**
// - align-items: `center`
// - cursor: `pointer`
// - display: `flex`
// - gap: `var(--spacing-xs)`
// - padding: `var(--spacing-xs) var(--spacing-sm)`
// - [+1 more layout properties]
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-sm)` 🎨
// - text-align: `left`
// **Effects:**
// - transition: `background-color var(--ui-duration-fast) var(--ui-ease-default)` 🎨
//
// **Interactions:**
// - `:hover`: Changes background to `var(--ui-color-surface-container-high)`
const QuerySidebarItem = "query-sidebar__item"

// **Layout:**
// - list-style: `none`
// - margin: `var(--spacing-xs) 0 0 0`
// - padding: `0`
const QuerySidebarList = "query-sidebar__list"

// **Visual:**
// - background: `var(--ui-color-surface-container-low)` 🎨
// - border-top: `var(--ui-border-sm) solid var(--ui-color-outline-variant)` 🎨
// **Layout:**
// - max-height: `300px`
// - overflow-y: `auto`
// - padding: `var(--spacing-md)`
const QuerySidebarSchema = "query-sidebar__schema"

// **Layout:**
// - flex: `1`
// - overflow-y: `auto`
// - padding: `var(--spacing-md)`
const QuerySidebarTables = "query-sidebar__tables"

// **Layout:**
// - align-items: `center`
// - display: `flex`
// - gap: `var(--spacing-xs)`
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
const SchemaPanelColName = "schema-panel__col-name"

// **Visual:**
// - color: `var(--ui-color-surface-variant-on)` 🎨
// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-xs)` 🎨
const SchemaPanelColType = "schema-panel__col-type"

// **Visual:**
// - border-bottom: `var(--ui-border-sm) solid var(--ui-color-outline-variant)` 🎨
// **Layout:**
// - align-items: `center`
// - display: `flex`
// - justify-content: `space-between`
// - padding: `var(--spacing-xs) 0`
// **Typography:**
// - font-size: `var(--ui-type-size-sm)` 🎨
//
// **Interactions:**
// - `:last-child`: Changes border-bottom to `none`
const SchemaPanelColumn = "schema-panel__column"

// **Layout:**
// - list-style: `none`
// - margin: `0`
// - padding: `0`
const SchemaPanelColumns = "schema-panel__columns"

// **Layout:**
// - align-items: `center`
// - display: `flex`
// - gap: `var(--spacing-sm)`
// - margin-bottom: `var(--spacing-sm)`
const SchemaPanelHeader = "schema-panel__header"

// **Typography:**
// - font-family: `var(--ui-font-code)` 🎨
// - font-size: `var(--ui-type-size-sm)` 🎨
// - font-weight: `var(--ui-weight-semibold)` 🎨
const SchemaPanelName = "schema-panel__name"

// **Visual:**
// - background: `var(--ui-color-primary)` 🎨
// - border-radius: `var(--ui-radius-sm)` 🎨
// - color: `var(--ui-color-primary-on)` 🎨
// **Layout:**
// - padding: `1px var(--ui-space-xs)` 🎨
// **Typography:**
// - font-size: `var(--ui-type-size-xs)` 🎨
// - font-weight: `var(--ui-weight-semibold)` 🎨
const SchemaPanelPk = "schema-panel__pk"

// **Visual:**
// - background: `var(--ui-color-surface-container-high)` 🎨
// - border-radius: `var(--ui-radius-sm)` 🎨
// - color: `var(--ui-color-surface-on)` 🎨
// **Layout:**
// - padding: `2px 6px`
// **Typography:**
// - font-size: `var(--ui-type-size-xs)` 🎨
// - text-transform: `uppercase`
const SchemaPanelType = "schema-panel__type"

